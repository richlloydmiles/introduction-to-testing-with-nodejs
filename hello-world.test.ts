import { expect } from '@jest/globals'

beforeEach(() => {
  jest.clearAllMocks()
})

describe('buildEmail', () => {
  test('buildEmail builds email as expected', async () => {
    expect(true).toBe(true)
  })
})

